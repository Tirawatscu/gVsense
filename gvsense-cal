#!/usr/bin/env python3
"""
gVsense Calibration CLI Tool
Command-line interface for managing oscillator calibration
"""

import sys
import os

# Add the current directory to Python path so we can import calibration_storage
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from calibration_storage import main

if __name__ == "__main__":
    sys.exit(main())
